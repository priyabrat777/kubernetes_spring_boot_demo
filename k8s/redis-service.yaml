apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: spring-boot-demo
  labels:
    app: redis
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: redis
